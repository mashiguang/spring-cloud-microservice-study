server:
  port: 8050
spring:
  application:
    name: microservice-config-server-eureka
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mashiguang/spring-cloud-study/
          search-paths: config-repo
          username:
          password:
eureka:
  client:
    service-url:
      defaultZone: http://discovery:8761/eureka/